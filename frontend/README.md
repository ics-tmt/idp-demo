# Jira Tickets by Story - Frontend

This is a minimal React-based UI to display Jira ticket counts grouped by story.

## Usage

1. Ensure the backend API is running on `http://localhost:8000`.
2. Open `index.html` in a web browser.

The page will fetch sample ticket data (modify as needed) and render a table and bar chart.
